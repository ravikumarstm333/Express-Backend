Express: FrameWork that is used on node for backend web devlopment


project structure :
Enviroment

 npm init -y  : use for Create package.json file make

 {
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",    -> that is main file name
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module"
}


npm i express dotenv   ||  npm install express dotenv:
both comamd for install express and dotenv-> Enviroment

After install see the dependencies in the package.json

{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {   -> here the dependencies it mens the module or dependencies require for for my project
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  }
}

npm install -D   nodemon -->  this is install for devlopment times


///now package.json
{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "app.mjs",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "nodemon ./src/app.mjs",   -> start server  comamd (npm run dev) or (npm start)
    "start": "nodemon ./src/app.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "dependencies": {
    "dotenv": "^17.4.2",
    "express": "^5.2.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14"
  }
}




Routing:
    Create endPoint/api and Handling request
    get request app.get('/',(req,res),{})
    app.All('/*splat',(req,res)=>{})


************************************************
Static File Handling
************************************************
  .html
  .css
  .png/jpg
  .js
if work in ESM:
  __dirname
  __filename
  the both are not working

Express Provide To handel the Static File
express Provide a middleware for static


if your Public Folder Contains a file by the name index.html then 
that file is automatic handel on root route by the static middleware


********************************************
GET and POST
********************************************